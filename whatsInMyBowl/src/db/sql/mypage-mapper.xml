<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

	<entry key="selectMainPageOrderList">
		SELECT
		       ORDER_CODE
		     , ORDER_AMOUNT
		     , ORDER_COMPANY
		     , ORDER_INVOICE
		     , ORDER_STATUS
		     , ORDER_DATE
		     , CU_NAME
		     , P_NAME
		     , P_MAINIMG
		     , PM_TOTALCOST
		  FROM "ORDER" OD
		  JOIN SUBORDER USING (ORDER_CODE)
		  JOIN PAYMENT USING (PM_CODE)
		  LEFT JOIN CUSTOM USING (CU_CODE)
		  LEFT JOIN PRODUCT USING (P_CODE)
		 WHERE OD.M_CODE = ?
           AND ORDER_DATE BETWEEN (SYSDATE - 30) AND SYSDATE
	</entry>
	
	<entry key="selectMainPagePostStatus">
		SELECT 
		       ORDER_STATUS
		     , COUNT(ORDER_STATUS) "COUNT"
		  FROM "ORDER"
		 WHERE M_CODE = ?
		   AND ORDER_DATE BETWEEN (SYSDATE - 30) AND SYSDATE
		 GROUP BY ORDER_STATUS
	</entry>

	<entry key="selectInquiryList">
		SELECT
		        I_CODE
		      , M.M_NAME 
		      , I_TITLE
		      , I_CATEGORY
		      , I_DATE
		      , I_ANSWER
		  FROM INQUIRY I
		LEFT JOIN MEMBER M ON (I.M_CODE = M.M_CODE)
		 ORDER
		    BY I_CODE DESC
	</entry>
</properties>